HOSTS:
  mgmt:
    roles:
      - master
      - agent
      - mgmt
    platform: el-6-x86_64
    hypervisor: docker
    image: centos:6
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'yum install -y tar wget'
    ip: 192.168.1.2
  meta:
    roles:
      - agent
      - meta
    platform: el-6-x86_64
    hypervisor: docker
    image: centos:6
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'yum install -y tar wget'
    ip: 192.168.1.3
  storage:
    roles:
      - agent
      - storage
    platform: el-6-x86_64
    hypervisor: docker
    image: centos:6
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'yum install -y tar wget'
    ip: 192.168.1.4
  client:
    roles:
      - agent
      - client
    platform: el-6-x86_64
    hypervisor: docker
    image: centos:6
    docker_preserve_image: true
    docker_cmd:
      - '/sbin/init'
    docker_image_commands:
      - 'yum install -y tar wget'
    ip: 192.168.1.5
CONFIG:
  log_level: debug
  type: foss
